<Window x:Class="YellowstonePathology.UI.NetMQTesting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YellowstonePathology.UI"
        mc:Ignorable="d"
        Title="NetMQTesting" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
            <Button  Margin="2,2,2,2" Height="30" Width="100" Click="ButtonStartPublish_Click">Start Publish</Button>
            <Button Margin="2,2,2,2" Height="30" Width="100" Click="ButtonStartSubscribe_Click">Start Subscribe</Button>
        </StackPanel>
        
        <ListView Grid.Column="0" Grid.Row="1" Margin="2,2,2,2" ItemsSource="{Binding Path=MessageList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Message" Width="500" DisplayMemberBinding="{Binding}" />
                </GridView>
            </ListView.View>
        </ListView>
        
    </Grid>
</Window>

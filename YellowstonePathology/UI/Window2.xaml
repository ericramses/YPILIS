<Window x:Class="YellowstonePathology.UI.Window2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YellowstonePathology.UI"
        mc:Ignorable="d"
        Title="Window2" Height="400" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" Width="100" Height="100" Click="Button_Click"></Button>
        
        <TextBlock Grid.Row="1" Text="{Binding Path=TouchDown}"></TextBlock>
        <TextBlock Grid.Row="2" Text="{Binding Path=TouchUp}"></TextBlock>

        <ListView Name="List" Grid.Row="3" Margin="2,2,2,2" ItemsSource="{Binding Path=TestList}" TouchDown="ListView_TouchDown" TouchUp="ListView_TouchUp" SelectionChanged="ListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Stuff" Width="300" DisplayMemberBinding="{Binding Path=.}" />
                </GridView>
            </ListView.View>
        </ListView>
        
    </Grid>
</Window>

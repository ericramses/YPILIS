<Window x:Class="YellowstonePathology.UI.DailyDOHDistributionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YellowstonePathology.UI"
        mc:Ignorable="d"
        Title="DailyDOHDistributionDialog" Height="600" Width="550">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>

            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Margin="2,2,2,2">State:</TextBlock>
        <ComboBox Name="ComboBoxDistributionType" Grid.Column="1" Grid.Row="0" ItemsSource="{Binding Path=DistributionTypes}" SelectionChanged="ComboBoxDistributionType_SelectionChanged">
        </ComboBox>

        <TextBlock Grid.Column="0" Grid.Row="1" Margin="2,2,2,2">Date:</TextBlock>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" Grid.Row="0" HorizontalAlignment="Stretch" Margin="2,2,2,2" Text="{Binding Path=DateAdded, Converter={StaticResource ShortDateConverter}}"></TextBox>

            <Button Grid.Column="1" Click="ButtonDateBack_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/Previous.ico" Margin="2" Height="16"></Image>
                </StackPanel>
            </Button>

            <Button Grid.Column="2" Click="ButtonDateForward_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/Next.ico" Margin="2" Height="16"></Image>
                </StackPanel>
            </Button>

        </Grid>

        <ListView Name="ListViewDistributions" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" ItemsSource="{Binding Path=TestOrderReportDistributionCollection}" Margin="2,2,2,2">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=ReportNo}" Header="Report No" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=DistributionType}" Header="Distribution Type" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=ClientName}" Header="Client" Width="200"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=TimeOfLastDistribution, Converter={StaticResource MilitaryDateTimeConverterV2}}" Header="Distributed" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3" HorizontalAlignment="Right">
            <Button Width="120" Height="30" Margin="2,2,2,2" Click="ButtonClose_Click">Close</Button>
        </StackPanel>

    </Grid>
</Window>

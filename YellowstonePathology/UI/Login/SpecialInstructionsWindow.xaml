<Window x:Class="YellowstonePathology.UI.Login.SpecialInstructionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Documents" Height="300" Width="300" WindowStyle="ToolWindow">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"></ColumnDefinition>		
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Margin="2,2,2,2" Foreground="SteelBlue">Special Instructions:</TextBlock>
        <TextBox Name="TextBoxSpecialInstructions" Grid.Column="0" Grid.Row="1" Margin="2,2,2,2" Text="{Binding Path=SpecialInstructions}"
                 HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="16" ></TextBox>
        
    </Grid>
</Window>
